import theme, { COLORS, SIZES, FONTS, darkTheme, lightTheme, selectedTheme, FontSize, Width, Height, ChangeDimention } from "./theme";
import images from "./images";
import icons from "./icons";
import data from "./data";


export {
    theme,
    COLORS,
    SIZES,
    FONTS,
    darkTheme,
    lightTheme,
    selectedTheme,
    images,
    icons,
    data, 
    FontSize,
    Width, 
    Height,
    ChangeDimention,
}

